<Window
    x:Class="PdfAnnotator.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PdfAnnotator"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="WinUI3 PDF Annotator"
    Height="860"
    Width="1200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Spacing="8" Padding="12" HorizontalAlignment="Left">
            <Button x:Name="OpenPdfButton" Content="打开 PDF" Click="OpenPdf_Click" />
            <TextBlock Text="画笔颜色" VerticalAlignment="Center" />
            <ColorPicker x:Name="InkColorPicker" MinWidth="160" IsAlphaEnabled="False" ColorChanged="InkColorChanged" />
            <TextBlock Text="画笔粗细" VerticalAlignment="Center" />
            <Slider x:Name="PenSizeSlider" Width="120" Minimum="1" Maximum="12" Value="3" ValueChanged="PenSizeChanged" />
            <ToggleButton x:Name="EraserToggle" Content="橡皮擦" Checked="EraserToggle_Checked" Unchecked="EraserToggle_Unchecked" />
            <TextBlock Text="橡皮粗细" VerticalAlignment="Center" />
            <Slider x:Name="EraserSizeSlider" Width="120" Minimum="2" Maximum="30" Value="12" ValueChanged="EraserSizeChanged" />
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel x:Name="PageHost" Orientation="Vertical" Spacing="18" Margin="12" />
        </ScrollViewer>
    </Grid>
</Window>
